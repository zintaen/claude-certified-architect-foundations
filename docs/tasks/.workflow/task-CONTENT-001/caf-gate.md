---
artefact: caf-gate@1
task: task-CONTENT-001
outcome: N/A
---

CAF_ENABLED=false in `.cyberos/gates.env`; no module `audit-profile.yaml` / `.caf/` baseline. Reduced-profile floor (build + lint + test) run via `run-gates.sh` instead — GREEN.
